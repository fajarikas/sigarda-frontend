import React from "react";

const Title = ({ text }) => {
  return (
    <div className="mt-7">
      <h1 className="font-semibold text-4xl lg:md:pl-0 pl-5 ">{text}</h1>
    </div>
  );
};

export default Title;
